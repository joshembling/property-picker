{
  "name": "property-picker",
  "version": "1.0.0",
  "scripts": {
    "start": "npx esbuild ./edit.js --bundle --outfile=dist/bundle.js --minify --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
